// Write a program to calculate square of a given number.Number is entered by the user.
#include<stdio.h>
int main()
{
    int a;
    printf("Enter a number to see its square: ");
    scanf("%d",&a);
    printf("%d^2=%d",a,a*a);
    return 0;
}