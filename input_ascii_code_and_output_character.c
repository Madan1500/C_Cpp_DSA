#include<stdio.h>
int main()
{
    char a;
    printf("Enter the ascii code for your character: ");
    scanf("%d",&a);
    printf("the character for the ASCII code %d is %c",a,a);
    return 0;
}